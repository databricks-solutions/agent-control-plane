# Screenshots

Add screenshots of the running app here. Recommended:

1. `governance.png` — Governance dashboard with cost charts
2. `agents.png` — Agent registry with topology graph
3. `observability.png` — MLflow experiments/traces cross-workspace view
4. `gateway.png` — AI Gateway usage analytics
5. `admin.png` — User analytics heatmap and RBAC matrix

Then uncomment the image references in the root `README.md`.

**Tip**: Use the browser's screenshot tool or `cmd+shift+4` on Mac to capture each page.
Recommended size: 1200px wide for good GitHub rendering.
